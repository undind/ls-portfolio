.app-textarea {
  width: 100%;
  resize: none;
  border: 1px solid $text-color;
  min-height: 150px;
  outline: none;
  padding: 20px;
  color: $text-color;
  font-weight: 600;
  line-height: 2;

  &:focus {
    border-color: $links-color;
  } 
}