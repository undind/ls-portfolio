.scroll {
  display: flex;
  justify-content: center;
  align-items: center;
  padding-bottom: 20px;
}

.scroll__desc {
  font-size: 18px;
  margin-right: 22px;
}

.scroll__icon {
  width: 25px;
  height: 30px;
  cursor: pointer;
}